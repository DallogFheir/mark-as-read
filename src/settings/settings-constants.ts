import { PLACEHOLDER } from "../common/constants";

export const ALPINE_DATA_ID = "app";

export const DEFAULT_TEST_URL = "https://www.test.com/page?q=123#section";

export const INVALID_CODE_ERROR =
  "The preprocessor code is invalid. Check the browser console for more details.";
export const INVALID_JSON_ERROR =
  "The provided JSON is invalid. It should be an array of objects with 'url' and 'datetime' properties.";

export const EXPORT_JSON_FILE_NAME = `mark-as-read-${PLACEHOLDER}.json`;

export const JSON_CONTENT_TYPE = "application/json";
